"""FastAPI service wrapping the profile and altitude analysers.

Repo-only, like ``cli/``: not part of the distributed wheel, so the
zero-runtime-dependency ``master_splinter`` package stays that way. Run with
``uvicorn api.main:app``.

"""
